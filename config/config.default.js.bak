'use strict';

module.exports = appInfo => {
    const config = exports = {
        sequelize: {
            database: 'demo',
            dialect: 'sqlite',
            storage: 'databases/database.sqlite',
        }
    };
    return config;
}